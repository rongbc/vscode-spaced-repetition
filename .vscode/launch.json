{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run extension (Spaced Repetition)",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "outFiles": [
                "${workspaceFolder}/out/**/*.js"
            ],
            "preLaunchTask": "Compile extension",
            "skipFiles": ["<node_internals>/**"]
        }
    ]
}
